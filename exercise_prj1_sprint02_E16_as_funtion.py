# Exercise 16 as a function

def get_length(text):
    print("The word - " + str(text) + " - has " + str(len(text)) + " characters.")

get_length("Hello")
get_length("Hi")

# concatenate error when trying to add text in print statement -> Why is that so?